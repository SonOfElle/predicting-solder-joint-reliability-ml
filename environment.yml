name: solder-reliability
channels:
  - conda-forge
  - defaults
dependencies:
  # Versions are pinned once the refactor is complete.
  # Exact RMSE reproduction depends on these pins.
  # See data/README.md, section "Known caveats".
  - python=3.10
  - pip
  - pip:
      # - tensorflow==
      # - scikit-learn==
      # - xgboost==
      # - numpy==
      # - pandas==
      # - matplotlib==
      # - jupyter==